enum AppointmentStatus {
  scheduled,
  checkedIn,
  inProgress,
  completed,
  cancelled,
  noShow,
}

enum ServiceStatus {
  pending,
  inProgress,
  completed,
  cancelled,
}

enum ServicePriority {
  low,
  normal,
  high,
  urgent,
}
